name: eds_test
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^4.0.0
  dio: ^5.1.2
  flutter_mobx: ^2.0.6+5
  mobx: ^2.2.0
  provider: ^6.0.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.5
  flutter_lints: ^1.0.0
  mobx_codegen: ^2.3.0

flutter:
  uses-material-design: true
